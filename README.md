# OnlineMessenger
A web-based messenger that allows users to register and communicate privately with text messages. The app uses Spring Boot as backend, PostgeSQL as data storage, HTML/CSS/JS for web interface. All dependencies and application itself are packaged in a Docker container.
